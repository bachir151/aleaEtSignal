package aleat.tpJ;

import aleat.tpD.Plotable;
import aleat.tpH.*;

import java.util.List;
import java.util.Random;

import java.util.ArrayList;
import aleat.tpB.*;

/**
 * Created by bachir on 12/01/16.
 */

//public class Ville implements aleat.tpH.Plotable {
  /* private ArrayList<Double> Xs = new ArrayList<Double>();
   private ArrayList<Double> Ys = new ArrayList<Double>();
   Random rand = new Random();
   MyRandom r = new MyRandom();
   int nbVilles;
   public Ville(int nbVilles) {
      this.nbVilles = nbVilles;
      Etat E = new Etat(1.,1);
      nbVilles = r.Poisson(100);
      Graphe G = new Graphe(nbVilles);
      for (int j = 0; j < G.Nbetats; j++) {
         int villeA = rand.nextInt(nbVilles);
         int villeB = rand.nextInt(nbVilles);
         double poids = (double) rand.nextInt(10);
         G.ajouteFleche(villeA, villeB, poids);
      }


      E.position(nbVilles);
      for (Etat etat : G.etats) {
         for (Fleche f : etat.sorties) {
            if (f.poids < 2) {
               Xs.add((double)E.pos.get(etat.Id));
               Ys.add((double)E.pos.get(etat.Id));

            }
         }
      }

   }
      public List<Double> getXs () {
         return Xs;
      }

      public List<Double> getYs () {
         return Ys;
      }
*/
 //  }
